<view class="exp-edit-page">
  <!-- 头部 -->
  <view class="page-header">
    <text class="back-btn" bindtap="goBack">←</text>
    <text class="page-title">发布经验</text>
  </view>

  <!-- 表单内容 -->
  <view class="page-content">
    <view class="form-item">
      <text class="label">标题</text>
      <input
        class="input"
        placeholder="如“孩子不爱吃饭，我是这样做的”"
        value="{{title}}"
        bindinput="onTitleInput"
        maxlength="40"
      />
    </view>

    <view class="form-item">
      <text class="label">内容</text>
      <textarea
        class="textarea"
        placeholder="详细说说你观察到的情况、尝试过的方法以及成果..."
        value="{{content}}"
        bindinput="onContentInput"
        maxlength="500"
      ></textarea>
      <text class="char-count">{{content.length}}/500</text>
    </view>

    <view class="hint-box">
      <text class="hint-title">小提示</text>
      <text class="hint-text">
        尽量描述具体一些。
      </text>
    </view>

    <button
      class="submit-btn"
      bindtap="submitPost"
      disabled="{{!canSubmit}}"
    >
      {{canSubmit ? '发布经验' : '填写标题和内容后可发布'}}
    </button>
  </view>
</view>

/* 优化后的个人信息卡片 */
.user-info-card {
  background: linear-gradient(135deg, #87CEEB, #4682B4);
  border-radius: 24rpx;
  padding: 32rpx;
  margin: 30rpx;
  box-shadow: 0 8rpx 32rpx rgba(102, 126, 234, 0.3);
  color: white;
}

/* 紧凑布局的宝宝信息 */
.baby-info-compact {
  display: flex;
  align-items: flex-start;
  margin-bottom: 30rpx;
}

.avatar-section {
  margin-right: 25rpx;
}

.avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #ff9a9e 0%, #fad0c4 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  border: 4rpx solid white;
  box-shadow: 0 4rpx 15rpx rgba(0, 0, 0, 0.1);
}

.avatar-image {
  width: 100%;
  height: 100%;
  border-radius: 50%;
}

.avatar-text {
  font-size: 40rpx;
  color: white;
  font-weight: bold;
}

.info-section {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
  min-height: 120rpx;
}

/* 第一行：名字和编辑按钮 */
.name-edit-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 15rpx;
}

.name-age {
  display: flex;
  align-items: baseline;
  flex: 1;
}

.baby-name {
  font-size: 36rpx;
  font-weight: bold;
  color: white;
  margin-right: 10rpx;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

.baby-age {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.9);
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

/* 编辑按钮 - 更明显 */
.edit-btn {
  display: flex;
  align-items: center;
  padding: 12rpx 20rpx;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 25rpx;
  border: 2rpx solid rgba(255, 255, 255, 0.5);
  transition: all 0.3s ease;
}

.edit-btn:active {
  background: rgba(255, 255, 255, 0.7);
  transform: scale(0.95);
}

.edit-icon {
  font-size: 24rpx;
  margin-right: 8rpx;
}

.edit-text {
  font-size: 24rpx;
  color: #4682B4;
  font-weight: 500;
}

/* 第二行：妈妈能量 */
.energy-row {
  display: flex;
  align-items: center;
}

.energy-label {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.9);
  margin-right: 15rpx;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

.energy-stars {
  display: flex;
  margin-right: 15rpx;
}

.star {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.4);
  margin-right: 5rpx;
  transition: all 0.3s ease;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

.star.active {
  color: #FFD700;
  transform: scale(1.1);
  text-shadow: 0 2rpx 6rpx rgba(255, 215, 0, 0.4);
}

.energy-desc {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
  font-style: italic;
  text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.2);
}

/* 签到区域 - 精简版 */
.checkin-section {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 20rpx;
  padding: 24rpx 32rpx;
  backdrop-filter: blur(10rpx);
}

.checkin-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(255, 255, 255, 0.9);
  border-radius: 50rpx;
  padding: 20rpx 36rpx;
  min-width: 180rpx;
  transition: all 0.3s;
  box-shadow: 0 4rpx 16rpx rgba(0, 0, 0, 0.1);
}

.checkin-btn:active {
  background: rgba(255, 255, 255, 0.8);
  transform: scale(0.95);
}

.btn-icon {
  font-size: 32rpx;
  margin-right: 12rpx;
}

.btn-text {
  font-size: 28rpx;
  font-weight: 600;
  color: #4682B4;
}

.checkin-stats {
  display: flex;
  align-items: center;
  padding: 16rpx 0;
}

.stats-icon {
  font-size: 28rpx;
  margin-right: 12rpx;
  opacity: 0.8;
}

.stats-text {
  font-size: 26rpx;
  opacity: 0.9;
  white-space: nowrap;
}
/* 浮动提示样式 */
.toast-message {
  position: fixed;
  top: 30%;
  left: 50%;
  transform: translateX(-50%) translateY(-20px);
  background: rgba(0, 0, 0, 0.8);
  color: white;
  padding: 20rpx 40rpx;
  border-radius: 50rpx;
  font-size: 28rpx;
  display: flex;
  align-items: center;
  opacity: 0;
  visibility: hidden;
  transition: all 0.3s ease;
  z-index: 9999;
}

.toast-message.show {
  opacity: 1;
  visibility: visible;
  transform: translateX(-50%) translateY(0);
}

.toast-icon {
  font-size: 32rpx;
  margin-right: 12rpx;
}

.toast-text {
  font-size: 28rpx;
}
/* 关心宝贝功能区 */
.care-section {
  background: #fff;
  border-radius: 16rpx;
  padding: 30rpx;
  margin: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.care-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #1a1a1a;
  margin-bottom: 30rpx;
  display: flex;
  align-items: center;
}

.care-title::before {
  content: '';
  display: inline-block;
  width: 6rpx;
  height: 30rpx;
  background: #4a90e2;
  border-radius: 3rpx;
  margin-right: 15rpx;
}

.function-grid {
  display: flex;
  justify-content: space-around;
}

.function-item {
  display: flex;
  flex-direction: column;
  align-items: center;
}

.function-icon {
  font-size: 60rpx;
  margin-bottom: 15rpx;
}

.function-text {
  font-size: 26rpx;
  color: #666;
}

/* 状态评分图表区域 */
.chart-section {
  background: #fff;
  border-radius: 16rpx;
  padding: 30rpx;
  margin: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
}

.chart-header {
  display: flex;
  align-items: center;
  margin-bottom: 60rpx; /* 从45rpx增加到60rpx，增加图表与标题的间距 */
  gap: 30rpx; /* 从20rpx增加到30rpx，增加标题与描述的间距 */
  padding: 0 10rpx; /* 添加内边距让布局更宽松 */
}

.chart-title {
  font-size: 34rpx; /* 稍微增大标题 */
  font-weight: 700; /* 加粗 */
  color: #1a1a1a;
  white-space: nowrap;
  flex-shrink: 0;
  letter-spacing: 0.5rpx; /* 字间距 */
}

.chart-desc {
  font-size: 26rpx; /* 稍微增大描述文字 */
  color: #666;
  line-height: 1.5;
  flex: 1;
  text-align: right;
  padding-left: 20rpx; /* 在描述文字左侧添加内边距 */
  border-left: 1rpx solid #f0f0f0; /* 添加分隔线 */
}

/* 紧凑图表容器 */
.compact-chart {
  position: relative;
  height: 180rpx; /* 稍微增加图表高度 */
  display: flex;
  flex-direction: column;
  margin-top: 10rpx; /* 图表上方额外间距 */
}

/* 图表区域 */
.chart-area {
  flex: 1;
  position: relative;
  margin: 0 30rpx;
  background: linear-gradient(to top, rgba(74, 144, 226, 0.05) 0%, transparent 100%);
  border-radius: 8rpx; /* 稍微增大圆角 */
  border-bottom: 1rpx solid #e8e8e8; /* 更细的边框 */
}

.chart-column {
  position: absolute;
  bottom: 0;
  transform: translateX(-50%);
  display: flex;
  flex-direction: column;
  align-items: center;
  height: 100%;
  width: 0;
}

/* 分数文本 */
.score-text {
  position: absolute;
  bottom: 100%;
  margin-bottom: 25rpx; /* 从20rpx增加到25rpx */
  font-weight: bold;
  line-height: 1;
  text-align: center;
  white-space: nowrap;
}

.current-score {
  font-size: 28rpx;
  color: #4a90e2;
  font-weight: 700;
}

.other-score {
  font-size: 22rpx;
  color: #666;
  font-weight: 500;
}

/* 数据点 */
.chart-dot {
  position: absolute;
  width: 14rpx;
  height: 14rpx;
  background: #4a90e2;
  border-radius: 50%;
  border: 2rpx solid white;
  box-shadow: 0 2rpx 6rpx rgba(74, 144, 226, 0.3); /* 增强阴影 */
  z-index: 2;
}

.chart-dot.low-dot {
  background: #F44336;
  box-shadow: 0 2rpx 6rpx rgba(244, 67, 54, 0.3);
}

.chart-dot.active-dot {
  width: 18rpx;
  height: 18rpx;
  background: #FFD700;
  box-shadow: 0 3rpx 8rpx rgba(255, 215, 0, 0.5); /* 增强阴影 */
  z-index: 3;
}

/* 日期显示 */
.dates-display {
  position: relative;
  height: 40rpx;
  margin-top: 25rpx; /* 从20rpx增加到25rpx */
  padding: 0 30rpx;
}

.date-column {
  position: absolute;
  bottom: 0;
  transform: translateX(-50%);
  display: flex;
  justify-content: center;
  width: 0;
}

.date-label {
  font-size: 20rpx;
  color: #999;
  text-align: center;
  white-space: nowrap;
  transform: scale(0.9);
}

.current-date {
  color: #4a90e2;
  font-weight: 500;
  transform: scale(1);
}

/* 今日快速记录模块 */
.quick-record-section {
  margin: 30rpx;
  padding: 32rpx;
  background: white;
  border-radius: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
  margin-bottom: 32rpx;
}

/* 快速记录入口 */
.quick-actions {
  display: flex;
  justify-content: space-between;
  gap: 20rpx;
}

.action-item {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32rpx 20rpx;
  background: white;
  border-radius: 16rpx;
  border: 2rpx solid #f0f0f0;
  transition: all 0.3s ease;
  min-height: 160rpx;
}

.action-item.recorded {
  border-color: #4dabf7;
  background: #f8f9fa;
  transform: translateY(-2rpx);
}

.action-icon {
  font-size: 56rpx;
  margin-bottom: 16rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.action-text {
  font-size: 28rpx;
  color: #333;
  font-weight: 500;
  margin-bottom: 8rpx;
}

.action-desc {
  font-size: 24rpx;
  color: #666;
  text-align: center;
  line-height: 1.4;
}

.action-placeholder {
  font-size: 24rpx;
  color: #999;
  text-align: center;
}
/* 快速记录模块标题行 */
.section-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 32rpx;
}

.section-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

/* 历史记录入口 */
.history-entry {
  display: flex;
  align-items: center;
  padding: 12rpx 20rpx;
  background: #f8f9fa;
  border-radius: 20rpx;
  border: 1rpx solid #e9ecef;
}

.history-text {
  font-size: 26rpx;
  color: #666;
  margin-right: 8rpx;
}

.history-icon {
  font-size: 28rpx;
  color: #999;
  font-weight: bold;
}

/* 经验交流模块 */
.experience-section {
  margin: 24rpx;
  padding: 24rpx 24rpx 12rpx;
  background: #ffffff;
  border-radius: 24rpx;
  box-shadow: 0 6rpx 18rpx rgba(0, 0, 0, 0.04);
}

.experience-header {
  flex-direction: row;
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.experience-header-left {
  flex: 1;
}

.experience-title-row {
  flex-direction: row;
  display: flex;
  align-items: center;
  margin-bottom: 4rpx;
}

.experience-title-bar {
  width: 8rpx;
  height: 32rpx;
  border-radius: 999rpx;
  background: #6A8BFF;
  margin-right: 12rpx;
}

.experience-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #333;
}

.experience-subtitle {
  font-size: 24rpx;
  color: #999;
}

.experience-more-wrap {
  flex-direction: row;
  display: flex;
  align-items: center;
  padding: 6rpx 4rpx;
}

.experience-more-text {
  font-size: 24rpx;
  color: #6A8BFF;
}

.experience-more-arrow {
  font-size: 28rpx;
  color: #6A8BFF;
  margin-left: 4rpx;
}

/* 卡片列表 */
.experience-card {
  margin-top: 12rpx;
  padding: 18rpx 16rpx 14rpx;
  background: #f9fbff;
  border-radius: 18rpx;
}

.experience-card + .experience-card {
  margin-top: 10rpx;
}

.card-title-row {
  margin-bottom: 6rpx;
}

.card-title {
  font-size: 28rpx;
  font-weight: 500;
  color: #333;
}

.card-meta-row {
  margin-bottom: 8rpx;
}

.card-author {
  font-size: 24rpx;
  color: #999;
}

.card-stat-row {
  flex-direction: row;
  display: flex;
  gap: 24rpx;
}

.card-stat {
  flex-direction: row;
  display: flex;
  align-items: center;
}

.card-stat-icon {
  font-size: 24rpx;
  margin-right: 4rpx;
}

.card-stat-text {
  font-size: 24rpx;
  color: #666;
}

/* 空状态 */
.experience-empty {
  margin-top: 16rpx;
}

.empty-title {
  font-size: 26rpx;
  color: #333;
  margin-bottom: 4rpx;
}

.empty-text {
  font-size: 24rpx;
  color: #999;
}

/* ===== 和我聊聊模块 ===== */
.chat-section {
  margin: 24rpx;
}

.chat-card {
  display: flex;
  flex-direction: row;
  align-items: center;
  padding: 26rpx 28rpx;
  background: linear-gradient(135deg, #87CEEB, #4682B4);
  border-radius: 24rpx;
  box-shadow: 0 6rpx 16rpx rgba(0, 0, 0, 0.08);
}

.chat-left {
  flex: 1;
}

.chat-title-row {
  flex-direction: row;
  display: flex;
  align-items: center;
  margin-bottom: 6rpx;
}

.chat-emoji {
  font-size: 30rpx;
  margin-right: 10rpx;
}

.chat-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #ffffff;
}

.chat-subtitle {
  font-size: 24rpx;
  color: #eaf0ff;
}

.chat-right {
  padding: 10rpx 24rpx;
  border-radius: 999rpx;
  background-color: #ffffff;
}

.chat-btn-text {
  font-size: 24rpx;
  color: #4682B4;
}

/* ===== 和我聊聊弹层 ===== */
.chat-modal-mask {
  position: fixed;
  left: 0;
  top: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0.45);
  z-index: 999;
  display: flex;
  justify-content: center;
  align-items: center;
}

.chat-modal {
  width: 80%;
  background: #ffffff;
  border-radius: 24rpx;
  padding: 24rpx 28rpx 30rpx;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.18);
}

.chat-modal-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16rpx;
}

.chat-modal-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #333333;
}

.chat-modal-close {
  font-size: 32rpx;
  color: #999999;
}

.chat-modal-body {
  margin-top: 4rpx;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.chat-modal-desc {
  font-size: 24rpx;
  color: #666666;
  text-align: center;
  margin-bottom: 18rpx;
}

/* 🎨 二维码占位色块 */
.qrcode-placeholder {
  width: 420rpx;
  height: 420rpx;
  border-radius: 20rpx;
  background: linear-gradient(135deg, #E0E7FF, #C7D2FE);
  display: flex;
  justify-content: center;
  align-items: center;
  margin-bottom: 10rpx;
}

.qrcode-placeholder-text {
  font-size: 26rpx;
  color: #4B5563;
}

.chat-modal-btn {
  margin-top: 16rpx;
  width: 100%;
  padding: 18rpx 0;
  border-radius: 999rpx;
  border: none;
  font-size: 28rpx;
  color: #ffffff;
  background-color: #4682B4;
}
/* 快速记录弹窗样式 */
.quick-record-modal {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1000;
}

.quick-record-modal .modal-mask {
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.5);
}

.quick-record-modal .modal-content {
  position: absolute;
  bottom: 0;
  left: 0;
  right: 0;
  background: white;
  border-radius: 24rpx 24rpx 0 0;
  padding: 40rpx;
  max-height: 70vh;
  overflow-y: auto;
}

.quick-record-modal .panel-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 40rpx;
}

.quick-record-modal .panel-title {
  font-size: 32rpx;
  font-weight: 600;
  color: #333;
}

.quick-record-modal .panel-close {
  font-size: 36rpx;
  color: #999;
  padding: 10rpx;
}

/* 情绪选项样式 */
.quick-record-modal .emotion-options {
  display: flex;
  justify-content: space-around;
  margin-bottom: 40rpx;
}

.quick-record-modal .emotion-option {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 30rpx 20rpx;
  border-radius: 20rpx;
  border: 2rpx solid #f0f0f0;
  transition: all 0.3s;
  min-width: 120rpx;
}

.quick-record-modal .emotion-option.selected {
  border-color: #6BC5D2;
  background: #f0f9fb;
  transform: scale(1.05);
}

.quick-record-modal .emotion-icon {
  font-size: 60rpx;
  margin-bottom: 16rpx;
}

.quick-record-modal .emotion-text {
  font-size: 28rpx;
  color: #666;
}

.quick-record-modal .emotion-option.selected .emotion-text {
  color: #6BC5D2;
  font-weight: 500;
}

/* 运动选项样式 */
.quick-record-modal .exercise-options {
  display: flex;
  flex-wrap: wrap;
  gap: 20rpx;
  margin-bottom: 40rpx;
}

.quick-record-modal .exercise-option {
  flex: 1;
  min-width: 120rpx;
  padding: 24rpx 16rpx;
  text-align: center;
  border: 2rpx solid #f0f0f0;
  border-radius: 12rpx;
  transition: all 0.3s;
  background: white;
}

.quick-record-modal .exercise-option.selected {
  border-color: #6BC5D2;
  background: #f0f9fb;
  color: #6BC5D2;
  font-weight: 500;
  transform: scale(1.05);
}

.quick-record-modal .exercise-text {
  font-size: 28rpx;
  font-weight: 500;
}

/* 弹窗按钮样式 */
.quick-record-modal .modal-actions {
  display: flex;
  gap: 20rpx;
  margin-top: 20rpx;
}

.quick-record-modal .btn-cancel,
.quick-record-modal .btn-confirm {
  flex: 1;
  padding: 24rpx;
  border-radius: 12rpx;
  font-size: 28rpx;
  font-weight: 500;
  border: none;
}

.quick-record-modal .btn-cancel {
  background: #f5f5f5;
  color: #666;
}

.quick-record-modal .btn-confirm {
  background: #6BC5D2;
  color: white;
}

/* 今日状态显示样式 */
.today-status {
  display: flex;
  gap: 20rpx;
  margin-bottom: 20rpx;
  padding: 20rpx;
  background: #f8f9fa;
  border-radius: 12rpx;
  flex-wrap: wrap;
}

.status-item {
  font-size: 26rpx;
  color: #666;
  padding: 8rpx 16rpx;
  background: white;
  border-radius: 20rpx;
  border: 1rpx solid #e8e8e8;
}

/* 快速记录按钮样式调整 */
.quick-actions {
  display: flex;
  justify-content: space-around;
  gap: 20rpx;
}

.action-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 20rpx;
  border-radius: 16rpx;
  background: white;
  box-shadow: 0 2rpx 8rpx rgba(0,0,0,0.06);
  transition: all 0.3s;
  flex: 1;
}

.action-item:active {
  background: #f8f9fa;
  transform: scale(0.98);
}

.action-icon {
  font-size: 48rpx;
  margin-bottom: 12rpx;
}

.action-text {
  font-size: 24rpx;
  color: #333;
  font-weight: 500;
}
/* 睡眠选项样式 */
.quick-record-modal .sleep-options {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
  margin-bottom: 40rpx;
}

.quick-record-modal .sleep-option {
  display: flex;
  align-items: center;
  padding: 30rpx;
  border-radius: 16rpx;
  border: 2rpx solid #f0f0f0;
  transition: all 0.3s;
  background: white;
}

.quick-record-modal .sleep-option.selected {
  border-color: #6BC5D2;
  background: #f0f9fb;
  transform: scale(1.02);
}

.quick-record-modal .sleep-icon {
  font-size: 48rpx;
  margin-right: 20rpx;
  width: 60rpx;
  text-align: center;
}

.quick-record-modal .sleep-text {
  font-size: 30rpx;
  color: #333;
  font-weight: 500;
  flex: 1;
}

.quick-record-modal .sleep-desc {
  font-size: 24rpx;
  color: #999;
  margin-left: 20rpx;
}

.quick-record-modal .sleep-option.selected .sleep-text {
  color: #6BC5D2;
}

.quick-record-modal .sleep-option.selected .sleep-desc {
  color: #6BC5D2;
}